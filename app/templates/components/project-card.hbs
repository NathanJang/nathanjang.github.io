<div class="project-card text-center">
  <img src={{project.iconPath}} alt={{project.iconAltText}} class="project-card-icon {{if project.shouldMaskIcon 'project-card-icon-masked'}}" aria-hidden="true">
  <h2 class="project-card-title">{{project.title}}</h2>
  {{#if project.caption}}
    <strong class="project-card-description">{{project.caption}}</strong>
  {{/if}}
  <p class="project-card-description mt-2">{{project.description}}</p>
</div>
